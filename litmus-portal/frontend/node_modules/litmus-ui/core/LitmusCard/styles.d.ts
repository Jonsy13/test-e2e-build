interface StyleProps {
    glow?: boolean;
    borderColor: string;
    height: string;
    width: string;
}
declare const useStyles: (props: StyleProps) => Record<"root", string>;
export { useStyles };
